about.js document.addEventListener('DOMContentLoaded', function () {  页面淡入效果 document.body.classList.add('fade-in');  内容逐级出现（可选增强） const elements = document.querySelectorAll('.profile p, .btn, .footer-note'); elements.forEach((el, index) => { el.style.opacity = '0'; setTimeout(() => { el.style.transition = 'opacity 0.8s ease, transform 0.6s ease'; el.style.opacity = '1'; if (el.classList.contains('btn')) { el.style.transform = 'translateY(0)'; } }, 300 + index * 150); });  按钮点击波纹效果（简化版） document.querySelectorAll('.btn').forEach(btn => { btn.addEventListener('click', function(e) {  阻止默认行为瞬间完成（仅用于视觉反馈） const rect = btn.getBoundingClientRect(); const ripple = document.createElement('span'); const size = Math.max(rect.width, rect.height); const x = e.clientX - rect.left - size / 2; const y = e.clientY - rect.top - size / 2; ripple.style.cssText = ` position: absolute; width: ${size}px; height: ${size}px; left: ${x}px; top: ${y}px; background: rgba(255, 255, 255, 0.4); border-radius: 50%; transform: scale(0); animation: ripple 0.6s linear; pointer-events: none; z-index: 1; `; this.appendChild(ripple); setTimeout(() => { ripple.remove(); }, 600); }); }); });  动态添加 CSS 动画（避免在 HTML 中写 style 标签） (function addRippleStyle() { if (document.getElementById('ripple-style')) return; const style = document.createElement('style'); style.id = 'ripple-style'; style.textContent = ` @keyframes ripple { to { transform: scale(2); opacity: 0; } } .fade-in { opacity: 1 !important; } `; document.head.appendChild(style); })();
