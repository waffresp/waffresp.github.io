 (function () { 'use strict';  document.addEventListener('DOMContentLoaded', function () { const loadingPercent = document.querySelector('.loading-percent'); const container = document.getElementById('loading-container'); const mainContent = document.getElementById('main-content');   if (!loadingPercent || !container || !mainContent) { console.warn('加载元素缺失，跳过加载动画'); if (mainContent) mainContent.classList.add('loaded'); return; } let percent = 0; let hasSwitched = false; function switchToMain() { if (hasSwitched) return; hasSwitched = true; container.style.opacity = '0'; setTimeout(() => { container.style.display = 'none'; mainContent.classList.add('loaded'); }, 500); } / const interval1 = setInterval(() => { if (percent >= 92) { clearInterval(interval1); loadingPercent.textContent = '92%'; setTimeout(() => {  const interval2 = setInterval(() => { percent++; loadingPercent.textContent = `${percent}%`; if (percent >= 100) { clearInterval(interval2);setTimeout(switchToMain, 300); } }, 100);  }, 1500); } else { percent++; loadingPercent.textContent = `${percent}%`; } }, 20);  setTimeout(switchToMain, 4000); }); })();